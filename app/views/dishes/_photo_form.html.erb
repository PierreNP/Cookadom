<div>
  <% if dish.photo.attached? %>
    <%=image_tag(dish.photo, alt: dish.name)%>
  <% else %>
    <p>aucun avatar</p>
  <% end %>
</div>

<%= form_tag dish_photos_path(dish), multipart: true do |f|%>
  <%= file_field_tag :photo %>
  <%= submit_tag "ajouter", class: "btn btn-primary" %>
<% end %>